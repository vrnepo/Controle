fastapi>=0.110
uvicorn[standard]>=0.29
itsdangerous>=2.1
python-dotenv>=1.0,<2
# Obrigatória para Form(...) e UploadFile — sem ela o FastAPI nem sobe, e a
# falha só aparece na importação do main.py (descoberto em 08/08/2026 ao
# importar as rotas pela primeira vez).
python-multipart>=0.0.9
# Banco: Neon (Postgres). Versões fixadas na mesma família certificada no
# SICAD — o Render instala "a mais nova", e uma major nova do psycopg sem
# wheel binária cairia na implementação pura, sem libpq.
sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.2,<3.3
# Leitura dos arquivos do banco. pdfplumber é o que permite ler a fatura do
# Santander, que vem em PDF protegido por senha e com layout de duas colunas.
pdfplumber>=0.11,<0.12
openpyxl>=3.1,<4
# Espelho para a planilha do Google — OPCIONAL. Sem GCP_SERVICE_ACCOUNT_JSON
# o sistema funciona inteiro, só não espelha.
gspread>=6.0
google-auth>=2.28
tzdata>=2024.1
